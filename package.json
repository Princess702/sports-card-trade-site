{
  "scripts": {
    "build": "node --check src/app.js",
    "dev": "python3 -m http.server 5173",
    "preview": "python3 -m http.server 4173",
    "check:conflicts": "node scripts/check-conflicts.mjs",
    "test": "npm run build && npm run check:conflicts"
  },
  "dependencies": {},
  "devDependencies": {}
}
